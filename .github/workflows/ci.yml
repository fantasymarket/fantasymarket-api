name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-go@v2-beta
      with:
        go-version: '^1.13.1' # The Go version to download and use.
    - run: go version
    - run: go mod verify
    - run: go test ./...
